import enum
#===============================================================================
# 工厂
#===============================================================================
class E型号(enum.IntEnum):
	s5750 = 5750
def f创建设备(a连接, a型号, a版本 = 0):
	if a连接.c连接特性 & 0x0001:	#命令行
		from .锐捷命令行 import 设备 as 设备
		return 设备.C设备(a连接, a型号, a版本)
	else:
		raise ValueError("参数错误")
