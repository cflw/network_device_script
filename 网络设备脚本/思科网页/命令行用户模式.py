import time
from ..思科命令行 import 用户模式
class C用户模式(用户模式.C用户模式):
	def __init__(self, a):
		用户模式.C用户模式.__init__(self, a)
	def f登录(self):
		time.sleep(0.5)
		self.m设备.f输入_结束符()
	def f模式_全局配置(self):
		from . import 命令行全局配置
		return 命令行全局配置.C全局配置(self)