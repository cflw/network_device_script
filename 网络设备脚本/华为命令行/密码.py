from ..命令行接口 import 设备 as 南向设备
from ..命令行接口 import 命令
def f加密等级(a):
	if type(a) == str:
		v补全 = 命令.f命令补全(a, 'cipher', 'simple')
		if v补全:
			return v补全
	if 南向设备.f参数等级(a加密等级, 1):
		return 'cipher'
	else:
		return 'simple'
